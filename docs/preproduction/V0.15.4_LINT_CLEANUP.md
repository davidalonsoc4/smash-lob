# Smash & Lob v0.15.4 — Lint cleanup

## Scope

- Removed an unused translation binding from `ProfileCompletionGate`.
- Preserved the translated onboarding form used by the nested profile-completion component.
- Updated visible application version and public changelog.

## Validation

- No API, route, permission, persistence, or database change.
- No Supabase migration required.
- Run `npm run lint`, `npx tsc --noEmit`, and `npm run build` before merging to staging.
