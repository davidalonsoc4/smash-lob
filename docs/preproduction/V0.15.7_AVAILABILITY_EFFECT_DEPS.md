# v0.15.7 — Availability effect dependency cleanup

## Scope

- Add the retry-label translation to the availability hydration effect dependency list.
- Keep the existing loading, retry, offline and persistence behavior unchanged.
- Update the visible application version and public changelog.

## Database

No migration is required.
