# v0.14.2 - Suggestions and settings search

## Scope

- Add an authenticated suggestion inbox under Settings > Help and information.
- Add a superuser-only review screen under Application administration.
- Replace the inline Settings search bar with a floating search control above the bottom navigation.
- Expand and improve the Settings search index and matching behavior.
- Compact the Match "More actions" menu so each action remains on one line.

## Database

New migration:

- `20260724111500_add_application_suggestions.sql`

The migration creates `application_suggestions`, enables RLS, removes browser grants, and leaves access through service-role server routes only.

## Routes

- `/settings/suggestions`
- `/application-admin/suggestions`
- `/api/suggestions`
- `/api/application-admin/suggestions`

## Validation required before merge

```powershell
npm run lint; if ($LASTEXITCODE -eq 0) { npx tsc --noEmit }; if ($LASTEXITCODE -eq 0) { npm run build }
```

Apply the migration only to the verified staging Supabase project before testing the suggestion flow.
