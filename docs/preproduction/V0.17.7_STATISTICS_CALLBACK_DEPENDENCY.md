# v0.17.7 - Statistics callback dependency cleanup

## Scope

- Replace the type-only reference to `statistics.closestMatch` with the shared `MatchData | null` type.
- Remove the `react-hooks/exhaustive-deps` warning from `useStatisticsWorkspace`.
- Preserve the exact runtime behavior and statistics calculations introduced in v0.17.6.

## Validation

- Run `npm run lint`.
- Run `npx tsc --noEmit`.
- Run `npm run build`.
- Confirm the statistics landing page and all detail routes still open correctly.

## Persistence and deployment

- No database migration.
- No API contract change.
- No permission change.
- No persisted data change.
