# v0.16.8 - Bottom navigation and panel accent cleanup

- Removed the blurred palette-coloured shadow projected above the bottom navigation.
- Replaced it with a one-pixel palette separator and a subtle inner highlight, preserving the active navigation gradient and safe-area behavior.
- Restored the Colorful accent strip in row-based panels whose opaque child rows covered the card background.
- Added a semantic `app-card-accent-reveal` treatment to Settings, league administration and per-day availability panels.
- Kept the v0.16.7 layered-background solution, so the strip remains clipped by the exact card radius and does not depend on global overflow rules.
- No migration, API contract, permission or persistence change is required.
