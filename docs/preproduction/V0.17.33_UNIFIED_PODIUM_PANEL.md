# v0.17.33 - Unified podium panel

## Scope

Make the podium preview and exported classification look like a single grouped panel, mirroring the visual treatment already used in the ranking screen.

## Changes

- The in-app podium preview is now rendered inside one shared bordered panel.
- The exported image uses one grouped podium container with internal separators instead of independent cards.
- Position, points and games-difference data remain unchanged.
- No other season-summary sections are modified in this pass.

## Technical notes

- No migrations are required.
- No API contracts or persisted data change.
- Changes are limited to season-summary rendering and version metadata.
